{
  "name": "postgresql",
  "json_class": "Chef::Role",
  "description": "Install Chaos PostgreSQL Servicepack.",
  "chef_type": "role",
  "run_list": [
    "recipe[postgresql::repo]",
    "recipe[postgresql::sudo]",
    "recipe[postgresql::server]"
   ]
}

